version: '3'
services:
    web:
        build: .
        stdin_open: true
        tty: true
        ports: 
            - "8090:1753"
        volumes:
            - "/app/node_modules" 
            - ".:/app"
